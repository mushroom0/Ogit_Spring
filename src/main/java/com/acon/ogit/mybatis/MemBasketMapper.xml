<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="membersbasket">
	<insert id="insert" parameterType="membasketDto">
		INSERT INTO MEMBERS_BASCKET 
		(seq, members_id, product_detail_seq, product_seq, product_name,bascket_count) 
		VALUES (MEMBERS_BASCKET_SEQ.NEXTVAL, #{members_id},#{product_detail_seq}, #{product_seq}, #{product_name}, #{bascket_count})
	</insert>
	<select id="getList" resultType="membasketDto">
		SELECT *
		FROM MEMBERS_BASCKET
	</select>
	<select id="getData" parameterType="string" resultType="membasketDto">
		 SELECT product_detail_seq, product_seq, product_name, product_count
		 FROM MEMBERS_BASCKET
		 WHERE members_id=#{members_id}
	</select>

</mapper>


