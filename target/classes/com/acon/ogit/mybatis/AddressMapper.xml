<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="address">
	<insert id="insert" parameterType="addressDto">
		INSERT INTO ADDRESS_BOOK (seq, members_id, name, recipient_name, 
		recipient_phone, zipcode, address, address_detail,is_default,regist_date) 
		VALUES(ADDRESS_BOOK_SEQ.NEXTVAL, #{members_id}, #{name}, #{recipient_name}, #{recipient_phone}, #{zipcode},#{address},
		#{address_detail},#{is_default},SYSDATE )
	</insert>
	<select id="getList" parameterType="string" resultType="addressDto">
		SELECT seq, members_id, name, recipient_name, 
		recipient_phone, zipcode, address, address_detail,is_default,regist_date
		FROM ADDRESS_BOOK
	</select>
	<select id="detail" parameterType="int" resultType="addressDto"> 
		SELECT seq, name, recipient_name, recipient_phone, zipcode, address, address_detail,is_default,regist_date,update_date
		FROM ADDRESS_BOOK
		WHERE seq=#{seq}
	</select>

</mapper>


